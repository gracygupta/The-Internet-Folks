const express = require("express");
const router = express.Router();


// @route   POST /
// @desc    Create role from the given data
// @access  Accessible to owners only
// router.post("/", );


// @route   GET /
// @desc    List all the created roles with pagination
// @access  Public
// router.get("/", );

module.exports = router;